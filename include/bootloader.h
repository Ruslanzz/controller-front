/**
  ******************************************************************************
  * @file    bootloader.h
  * @brief   Сопряжение с CAN-загрузчиком Katapult.
  *
  *          Собирается только при -D USE_KATAPULT_BOOTLOADER (окружение
  *          `katapult` в platformio.ini). В обычной сборке под ST-Link обе
  *          функции превращаются в пустышки, и прошивка работает как раньше.
  *
  *          Katapult занимает первые 8 КБ flash, приложение живёт с 0x08002000
  *          и обязано перенести туда таблицу векторов. Запрос на обновление
  *          приходит по CAN от ведущего; узел кладёт сигнатуру в отведённые
  *          Katapult последние 8 байт ОЗУ и перезагружается.
  ******************************************************************************
  */

#ifndef __BOOTLOADER_H
#define __BOOTLOADER_H

#ifdef __cplusplus
extern "C" {
#endif

#include <stdint.h>

/**
 * Перенести таблицу векторов на адрес приложения. Вызывать САМЫМ ПЕРВЫМ в
 * main(), до HAL_Init() и любой инициализации периферии: до этого момента
 * прерывания уходили бы в таблицу загрузчика.
 */
void Bootloader_RelocateVectors(void);

/**
 * Перезагрузиться в Katapult: положить сигнатуру запроса в последние 8 байт
 * ОЗУ (адрес берётся из вектора начального указателя стека загрузчика) и
 * выполнить сброс. Не возвращается.
 *
 * Свет при этом гаснет немедленно и не восстановится, пока узел не выйдет из
 * режима обновления. Вызывать только по явной команде с проверенной
 * сигнатурой (см. can_bus.c) и на стоящей машине.
 */
void Bootloader_Request(void);

#ifdef __cplusplus
}
#endif

#endif /* __BOOTLOADER_H */
